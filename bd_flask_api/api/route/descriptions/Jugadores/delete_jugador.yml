Borra un jugador en especifico de la base de datos.
  ---
parameters:
  - in: path
    name: id_jugador
    description: El id del jugador que se quiere borrar.
    schema:
      type: integer
    required: true
responses:
  200:
      description: Te devuelve el jugador que se borro.