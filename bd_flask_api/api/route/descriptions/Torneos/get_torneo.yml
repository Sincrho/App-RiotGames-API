Devuelve un torneo en especifico de la base de datos.
  ---
parameters:
  - in: path
    name: id_torneo
    description: El id del torneo que se quiere buscar.
    schema:
      type: integer
    required: true
responses:
  200:
      description: Te devuelve el torneo que se buscaba.