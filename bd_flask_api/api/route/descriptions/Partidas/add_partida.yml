Agrega una partida a la base de datos.
  ---
parameters:
  - in: body
    name: Partida
    description: La partida a crear, enviar null en id_partida.
    schema:
      type: object
      properties:
        id_partida:
          type: integer
          value: null
        resultado_partida:
          type: string
    required: true
responses:
  200:
      description: Te devuelve la misma partida agregada.